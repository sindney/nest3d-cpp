#ifndef UTILS_H
#define UTILS_H

namespace nest
{
	class utils
	{
	public:
		
		static bool printLog(const char *log);

		static char* readTextFile(const char *fileName);

	};
}

#endif